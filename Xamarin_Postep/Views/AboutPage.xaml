<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.AboutPage"
             xmlns:vm="clr-namespace:Xamarin_Postep.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        
        
        <AbsoluteLayout Grid.Row="2" HorizontalOptions="EndAndExpand" VerticalOptions="EndAndExpand" >
            <Button ImageSource="icon_plus.png" CornerRadius="90" WidthRequest="50" Margin="10"   />

        </AbsoluteLayout>

        <AbsoluteLayout Grid.Row="2" HorizontalOptions="StartAndExpand" VerticalOptions="EndAndExpand" >
            <Button ImageSource="icon_list.png" Clicked="ShowApp_Click"  CornerRadius="90" WidthRequest="50" Margin="10"/>
        </AbsoluteLayout>
    </Grid>

</ContentPage>
