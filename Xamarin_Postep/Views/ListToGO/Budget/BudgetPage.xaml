<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.ListToGO.Budget.BudgetPage"
             xmlns:models="clr-namespace:Xamarin_Postep.Models">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackLayout>
            <Label Text="Wydatek" Grid.Column="0" Grid.Row="0" HorizontalOptions="Center" FontSize="Title"/>
            <CollectionView Grid.Column="0" Grid.Row="0"  ItemsSource="{Binding WydatekList}" Margin="5">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate >
                    <DataTemplate>
                        <Frame  Padding="2" x:DataType="models:Summary" BorderColor="Black">
                            <StackLayout Orientation="Vertical" Margin="5"  >
                                <StackLayout >
                                    <Label Text="{Binding Category}"  />
                                    <Label Text="{Binding Price}"  />
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            </StackLayout>

            <StackLayout Grid.Column="1" Grid.Row="0">
                <Label Text="Przychod" Grid.Column="0" Grid.Row="0" HorizontalOptions="Center" FontSize="Title"/>
                <CollectionView Grid.Column="1" Grid.Row="0" ItemsSource="{Binding PrzychodList}" Margin="5">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate >
                        <DataTemplate>
                            <Frame  Padding="2"  x:DataType="models:Summary" BorderColor="Black">
                                <StackLayout Orientation="Vertical" Margin="5"  >
                                    <StackLayout >
                                        <Label Text="{Binding Category}"  />
                                        <Label Text="{Binding Price}"  />
                                    </StackLayout>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>