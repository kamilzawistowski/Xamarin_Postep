<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:xc="clr-namespace:XCalendar;assembly=XCalendar"
             x:Class="Xamarin_Postep.Views.ListToGO.CalendarPage"
            xmlns:controls="clr-namespace:Xamarin.Plugin.Calendar.Controls;assembly=Xamarin.Plugin.Calendar">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
        <StackLayout>
        <controls:Calendar Grid.Row="0"
            SelectedDate="{Binding SelectedDate}"
                           x:Name="Calendar"
            Day="14"
            Month="5"
            Year="2019"
            VerticalOptions="FillAndExpand"
            HorizontalOptions="FillAndExpand"/>
        </StackLayout>
            <StackLayout BackgroundColor="Gray" Grid.Row="1">
                <Label Text="test" HorizontalOptions="Center" FontSize="Title"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>