<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.ListToGO.Language.English.EnglishNewWordPage"
             xmlns:models="clr-namespace:Xamarin_Postep.Models"
             xmlns:local="clr-namespace:Xamarin_Postep.ViewModels.Language">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior 
            Command="{Binding BackButton}"/>
    </Shell.BackButtonBehavior>
    
    <ContentPage.BindingContext>
        <local:EnglishWordsViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>

        <StackLayout>
            <Label Text="English"  FontSize="Medium" TextColor="Black" />
            <Entry Text="{Binding WordEnglish.WordEnglish,Mode=TwoWay}" FontSize="Small" x:Name="EnglishWordEntry" />
            <Label Text="Polskie" FontSize="Medium" TextColor="Black"/>
            <Entry Text="{Binding WordEnglish.WordPolish,Mode=TwoWay}" FontSize="Small" x:Name="PolishWordEntry"/>

            <Button Text="Potwierdz" Command="{Binding AddWordCommand}" Style="{StaticResource ButtonBackgroundColor}" CommandParameter="{Binding WordEnglish}" Clicked="Button_Clicked" ></Button>

            <Label Text="Kategoria" FontSize="Medium" TextColor="Black" />
            <Picker Title="Wybierz Kategorie" x:DataType="local:EnglishWordsViewModel" ItemsSource="{Binding Options,Mode=TwoWay}" SelectedItem="{Binding WordEnglish.Category}" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>