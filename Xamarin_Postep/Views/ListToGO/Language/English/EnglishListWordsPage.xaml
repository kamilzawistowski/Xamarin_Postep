<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.ListToGO.Language.English.EnglishListWordsPage"
             xmlns:models="clr-namespace:Xamarin_Postep.Models">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior 
            Command="{Binding BackButton}"/>
    </Shell.BackButtonBehavior>



    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Picker x:Name="categoryPicker" Title="Select Category" ItemsSource="{Binding Options}" SelectedItem="{Binding CategorySelected}" />


            <CollectionView ItemsSource="{Binding AllWords}"  Margin="10" SelectionMode="Multiple">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate >
                    <DataTemplate>
                        <Frame BorderColor="Black" Padding="10" BackgroundColor="AntiqueWhite" x:DataType="models:EnglishWord">
                            <StackLayout Orientation="Vertical" Margin="0"  >
                                <Label Text="{Binding Category}" HorizontalOptions="Start" FontSize="Small" FontAttributes="Italic"  TextColor="Black" />
                                <Frame BorderColor="Black">
                                    <StackLayout Orientation="Horizontal" >
                                        <Label Text="{Binding WordEnglish}" HorizontalOptions="StartAndExpand" FontAttributes="Bold" FontSize="Medium" />
                                        <Label Text="{Binding WordPolish}" FontAttributes="Bold" HorizontalOptions="CenterAndExpand" FontSize="Medium"/>
                                    </StackLayout>
                                </Frame>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>


            <Button Margin="10" Text="Delete Selected" Command="{Binding DeleteSelectedCommand}" Style="{StaticResource ButtonBackgroundColor}" Clicked="DeleteItems"/>
        </StackLayout>

    </ContentPage.Content>
</ContentPage>