<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.ListToGO.Habits.HabitsPage"
              xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid"
            xmlns:conv="clr-namespace:DataGridSample.Views.Converters;assembly=DataGridSample"
             xmlns:models="clr-namespace:Xamarin_Postep.Models"
             >
    <ContentPage.Content>
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
            
        <StackLayout Grid.Row="0">
            <Label Text="Lista Nawyków" HorizontalOptions="Center" FontSize="Title"/>
        </StackLayout>

            <!--<CollectionView Grid.Row="0"  ItemsSource="{Binding Habits}" Margin="5" SelectionMode="Single" SelectedItem="{Binding SelectedIcon}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal"   ItemSpacing="20"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate >
                    <DataTemplate>
                        <Grid Padding="10">
                            <Frame Padding="2"  BorderColor="Black">
                                <StackLayout Orientation="Horizontal" Margin="5"  >
                                    <Image Source="{Binding IconHabit}"/>
                                </StackLayout>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>-->
            <dg:DataGrid Grid.Row="1" ItemsSource="{Binding Habits}">
                <!--<dg:DataGrid.HeaderFontSize>
                    --><!--<OnIdiom  x:TypeArguments="x:Double">
                        <OnIdiom.Tablet>15</OnIdiom.Tablet>
                        <OnIdiom.Phone>13</OnIdiom.Phone>
                    </OnIdiom>--><!--
                </dg:DataGrid.HeaderFontSize>-->
                <dg:DataGrid.Columns>
                    <dg:DataGridColumn Title="Image" PropertyName="Logo" Width="100" x:DataType="models:Habit">
                        <dg:DataGridColumn.CellTemplate>
                            <DataTemplate>
                                <Image HorizontalOptions="Center" VerticalOptions="Center" Aspect="AspectFit" HeightRequest="60" >
                                    <Image.Source>
                                        <FileImageSource File="{Binding Image}" />
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </dg:DataGridColumn.CellTemplate>
                    </dg:DataGridColumn>
                    <dg:DataGridColumn Title="Name" PropertyName="Name" Width="2*"/>
                    <dg:DataGridColumn Title="Win" PropertyName="Win" Width="0.95*"/>
                    <!--<dg:DataGridColumn Title="Loose" PropertyName="Loose"  Width="1*"/>
                    <dg:DataGridColumn Title="Home" PropertyName="Home"/>
                    <dg:DataGridColumn Title="Percentage" PropertyName="Percentage" StringFormat="{}{0:0.00}" />-->
                    <!--<dg:DataGridColumn Title="Streak" PropertyName="Streak" Width="0.7*">
                        <dg:DataGridColumn.CellTemplate>
                            <DataTemplate>
                                <ContentView HorizontalOptions="Fill" VerticalOptions="Fill" 
                                        BackgroundColor="{Binding Streak,Converter={StaticResource StreakToColorConverter}}">
                                    <Label Text="{Binding Streak}" HorizontalOptions="Center" VerticalOptions="Center" TextColor="Black"/>
                                </ContentView>
                            </DataTemplate>
                        </dg:DataGridColumn.CellTemplate>
                    </dg:DataGridColumn>-->
                </dg:DataGrid.Columns>
                <dg:DataGrid.RowsBackgroundColorPalette>
                    <dg:PaletteCollection>
                        <Color>#F2F2F2</Color>
                        <Color>#FFFFFF</Color>
                    </dg:PaletteCollection>
                </dg:DataGrid.RowsBackgroundColorPalette>
                <dg:DataGrid.Resources>
                    <ResourceDictionary>
                        <!--<conv:StreakToColorConverter x:Key="StreakToColorConverter"/>-->
                    </ResourceDictionary>
                </dg:DataGrid.Resources>
            </dg:DataGrid>


            <Button Grid.Row="2"  HorizontalOptions="Start" VerticalOptions="End" Margin="20" ImageSource="icon_list.png" CornerRadius="90" Clicked="BackToPrevious_click"/>
            <Button Grid.Row="2"  HorizontalOptions="End" VerticalOptions="End" Margin="20" ImageSource="icon_plus.png" CornerRadius="90" Clicked="AddNewHabit_Click"/>
       
        </Grid>
    </ContentPage.Content>
</ContentPage>