<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Postep.Views.ListToGO.Workouts.WorkoutsPage"
             xmlns:local="clr-namespace:Xamarin_Postep.ViewModels.Workout"
             xmlns:Models="clr-namespace:Xamarin_Postep.Models">
    <ContentPage.BindingContext>
        <local:WorkoutsViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        
            <RefreshView x:DataType="local:WorkoutsViewModel"  Grid.Row="0" >
            <CollectionView ItemsSource="{Binding WorkoutsView}" SelectionMode="Single" Margin="2" >
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                
                                <StackLayout x:DataType="Models:Workout" Margin="15">
                                    <Label Text="{Binding DateTime}"/>
                                    <Label Text="{Binding Name}"/>
                                </StackLayout>
                                
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
            </RefreshView>
    </ContentPage.Content>
</ContentPage>